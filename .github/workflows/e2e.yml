name: e2e

# Playwright against Compose. Skipped until Docker is part of the builder's loop.
# GitHub-hosted runners have Docker; flip `if` to run on PRs to main once Compose is proven.

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * *"

jobs:
  e2e:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Enable after Docker Desktop / Compose is in use."
